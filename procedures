DROP PROCEDURE IF EXISTS sp_loginUser;

DELIMITER $$
CREATE  PROCEDURE `sp_loginUser`(
    IN p_username VARCHAR(45),
    IN p_password VARCHAR(93)
)
BEGIN
   select * from tbl_user where user_username = p_username and user_password = p_password;
END$$
DELIMITER ;